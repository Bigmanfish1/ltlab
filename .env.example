# ─── Local development (docker-compose) ───
# Database
POSTGRES_DB=ltlab
POSTGRES_USER=ltlab
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgres://ltlab:change-me@db:5432/ltlab

# Django
SECRET_KEY=generate-a-real-secret-key
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.development
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Supabase (auth) — Supabase → Settings → API
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=<anon-public-key>

# ─── Production (Cloud Run) ───
# Set on the Cloud Run service AND the ltlab-migrate Job (see infra/bootstrap.sh).
# Never committed — listed here only to document what the deployment requires.
#   DJANGO_SETTINGS_MODULE=config.settings.production
#   DEBUG=False
#   ALLOWED_HOSTS=.run.app                       # leading dot matches the Cloud Run host
#   SECRET_KEY=<python -c "import secrets; print(secrets.token_urlsafe(50))">
#   DATABASE_URL=<Supabase transaction pooler URI, port 6543 (IPv4)>  # needs DISABLE_SERVER_SIDE_CURSORS
#   SUPABASE_URL / SUPABASE_ANON_KEY             # same project as local
