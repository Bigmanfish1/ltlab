Django>=5.1,<6.0
psycopg[binary]>=3.2
celery[redis]>=5.4
django-environ>=0.11
whitenoise>=6.8
django-ninja>=1.3
gunicorn>=22.0
ruff>=0.4
# Pinned to <3 — views._pop_pkce_verifier relies on supabase-py/gotrue 2.x
# internal storage attrs (_storage_key/_storage); a major bump can break login.
supabase>=2.0,<3.0
gotrue>=2.0,<3.0
httpx>=0.27
# Local ES256 verification of Supabase access tokens (apps.accounts.jwt_auth);
# [crypto] pulls cryptography for the EC signature check.
PyJWT[crypto]>=2.8,<3.0